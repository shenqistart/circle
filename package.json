{
  "name": "circle",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "api": "uv run --project server uvicorn app.main:app --app-dir server --host 127.0.0.1 --port 8787 --reload",
    "dev": "pnpm --filter @circle/frontend dev",
    "build": "pnpm --filter @circle/frontend build",
    "typecheck": "pnpm --filter @circle/frontend typecheck",
    "test": "pnpm --filter @circle/frontend test",
    "test:api": "uv run --project server pytest server/tests",
    "test:watch": "pnpm --filter @circle/frontend test:watch",
    "graph:install": "uv venv .graphify-venv && uv pip install --python .graphify-venv/bin/python graphifyy && .graphify-venv/bin/graphify install --platform codex",
    "graph:build": ".graphify-venv/bin/graphify update .",
    "graph:update": ".graphify-venv/bin/graphify update .",
    "graph:query": ".graphify-venv/bin/graphify query"
  }
}
